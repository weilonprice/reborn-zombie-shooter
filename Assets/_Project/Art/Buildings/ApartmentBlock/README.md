# ApartmentBlock

Stylized low-poly survival arena building for the Zombie Shooter map.

- `ApartmentBlock.fbx` — Unity environment import.
- `ApartmentBlock.glb` — portable geometry export.
- `ApartmentBlock.blend` — editable Blender source in ArtSource.

The arena builder adds static mesh colliders when it places this building.
