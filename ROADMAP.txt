REBORN - DEVELOPMENT ROADMAP
Top-down twin-stick wave survival / Unity 6000.6.0f1 / URP
Target: commercial Steam release. Cadence: full-time (~32 hrs/wk). 64 weeks.
Status:  Gates 0 and 1 PASSED. Phase 2 content production underway - bigger
         arena, deployables, and data-driven weapon upgrade trees in, with the
         economy repriced so the trees are a choice. Direction settled: ten
         weapons, ONE upgrade path each, and the run's decision is which
         weapon to max. Weapon roster complete at 10 with a path each, enemy
         roster complete at 12 plus the boss, all four firing modes live, and
         the horde paths around geometry. What is left in Phase 2 is arenas,
         meta progression and performance. No art until the content exists.

Task list only. Each phase ends at a gate - do not start the next phase
until the gate passes.

Markers:  [x] done    [~] partly done    [ ] not started


-------------------------------------------------------------------------------
PROGRESS LOG
-------------------------------------------------------------------------------

2026-09-08  Player feel verified. Movement, shooting, aiming and reloading
            all confirmed good in play. No tuning adjustments needed.
2026-09-08  Enemies live. Zombies spawn on the ring, take damage, die, and
            recycle through the pool. Core loop is now closed end to end.
2026-09-08  Fixed: ArenaBuilder wrote a null zombie prefab reference because
            object references do not survive NewScene. Builder now generates
            assets and builds the scene in two passes, and validates the
            result instead of failing silently.
2026-09-08  Impact feedback in and verified. Hit flash and knockback on every
            hit, 50ms hit-stop gated to kills, death pop, muzzle flash and
            impact sparks. Bodies now linger briefly after death so the flash
            and freeze have something on screen to land on.
2026-09-08  First three sounds in. Gunshot, impact and death, synthesized as
            placeholders rather than sourced. AudioListener moved from the
            camera to the player: 20 units up, every ground-level sound sat
            at the far end of its distance rolloff.
2026-09-08  Screen shake in. Trauma-based with a squared response curve, so
            sustained fire stays subtle while kills and hits punctuate it.
            Camera split into a follow rig with the camera as its child, so
            shake is a local offset and cannot fight the follow logic.
2026-09-08  Player damage shake added, scaled by fraction of the health bar
            lost rather than a flat amount, so it stays correct as enemy
            damage and player max health change.
2026-09-08  Fixed: point-blank shots passed through zombies. The muzzle sits
            1.35 units out, an attacking zombie's surface at 1.18, so the ray
            began inside the target's collider - which Unity never reports as
            a hit. Hit test now starts at the player's centre; the tracer is
            still drawn from the barrel.
2026-09-08  Shell ejection and camera recoil kick in. Recoil lives on
            CameraShake rather than a second component, so nothing else
            competes for the camera's local transform.
2026-09-08  Ambient bed in. 12-second loop, tonal frequencies snapped to exact
            multiples of the loop fundamental and the noise layer wrap-
            crossfaded, so the seam is inaudible. Phase 0 tasks complete.
2026-09-08  GATE 0 PASSED. Run with an outside player, good feedback.
2026-09-08  Weapons are data. WeaponDefinition ScriptableObject holds every
            stat; Weapon is now only a runtime executing one against scene
            wiring. A new weapon is an asset, never a script.
2026-09-08  Four weapons built and switchable on 1-4: pistol, shotgun,
            assault rifle, sniper. Three systems went in to make them
            data-only - DamageInfo carrying a knockback multiplier,
            penetration with per-weapon falloff, and per-slot ammo banking
            so swapping cannot be used as a free reload.
2026-09-08  Fixed: shotgun spread was invisible. Every pellet drew into the
            same LineRenderer, so eight overwrote one line and only the last
            showed. Tracers now come from a pool, one line per pellet. Spread
            also made horizontal-dominant - equal vertical spread was throwing
            pellets over the zombies' heads in a top-down game.
2026-09-08  First enemy archetype in: the Brute. 300 HP, 1.6 move speed,
            heavy knockback resistance (1.2 vs 4.0), 25 damage melee, and 1.4x
            larger crimson silhouette. WaveManager updated with multi-
            prefab pooling; Brutes spawn starting on wave 2 (15% scaling to
            40%). Forces player to use shotgun knockback or sniper burst rather
            than relying solely on assault rifle spray.
2026-09-08  Runner and Ranged enemy archetypes in. Runner: 40 HP, 4.5 move
            speed, 6 damage, high knockback vulnerability (6.0), amber 0.75x
            scale. Ranged: 60 HP, 1.8 move speed, 11m preferred distance,
            purple 0.9x scale, fires 12 m/s linear projectiles dealing 15
            damage with sphere-sweep collision and pooled reuse. WaveManager
            now manages 4-way pooling and escalating archetype weights. All
            four weapons now have distinct counters: Shotgun stops Brutes,
            Rifle sprays Runners, Sniper picks off Ranged, Pistol fallbacks.
2026-09-08  Design locked: Tactical Gunsmith & Fortifications (Option 1).
            Core hook established: kill zombies to earn Gold, spend at the
            inter-wave Armory on weapon purchases, weapon mod cores, and
            deployable fortifications (destructible barricades, explosive red
            barrels, claymores) to physically engineer killboxes. Modernizing
            Boxhead's iconic barricade/barrel emergent sandbox with deep
            weapon build variety.
2026-09-08  Gold Economy & HUD in. ZombieAI awards gold on kill scaled by
            archetype (Standard: 10g, Runner: 15g, Ranged: 25g, Brute: 40g).
            GameManager banks run-persistent gold with TrySpendGold/AddGold
            APIs prepared for shop purchases. HUD displays golden wallet
            counter under score and records final gold on Game Over screen.
2026-09-08  Inter-wave Armory Shop in. Interactive break state with Time.timeScale
            pausing between waves. Player spawns with Pistol and purchases
            Shotgun ($150), Assault Rifle ($250), Sniper ($350), full Ammo Crates
            ($50), and 5 tangible Weapon Mod Cores (Drum Mags, Overclocked
            Receiver, Bore Piercing, Heavy Slug, Dragon's Breath) with accumulated
            gold.
2026-09-08  Pistol ammo updated from infinite to finite (120 reserve / 10 spare mags),
            restocked alongside all weapons via Armory Ammo Crates ($50).
2026-09-08  Debt list audited against the code. Item 6 had been overwritten by
            a barricade note and marked resolved; the O(n^2) separation it
            described was never fixed and has since gained a per-zombie
            per-frame spherecast on top. Restored, and three unrecorded items
            added: mods hardcoded by slot index, Armory prices as compile-time
            constants, and the silent-singleton pattern spreading to 27 sites.
2026-09-08  Fixed: Armory ammo crates refilled to the unmodded magazine size.
            Capacity was computed in two places - Weapon applied the drum mag
            multiplier while WeaponLoadout read the raw definition - so an
            upgraded rifle topped to 30 when it holds 45, silently half
            refunding the mod on every restock, on every weapon.
            ArmoryManager.EffectiveMagazineSize is now the single owner.
2026-09-08  Endless mode in. [E] on the victory screen drops the wave cap and
            resumes the same run at wave 16 rather than restarting - health,
            gold, weapons and mods all carry over, so it reads as continuing
            past the finish line rather than a separate mode. WaveNumber is a
            field and survives the coroutine ending, which is what makes the
            continuation free. The unlock is session-only until the save
            system lands in Phase 3.
2026-09-08  Boss built. Rather than a second AI, BossController switches an
            ordinary ZombieAI between behaviours it already supported:
            isRanged for the artillery phase, then melee charger at 50%
            health. That reused steering, separation, knockback, pooling and
            barricade sensing wholesale - the boss cost one small script plus
            five setters on ZombieAI. 2000 HP, 2.2x scale, knockbackForce
            0.35 against a standard 4.0 so the shotgun cannot hold it off.
            The phase-two barricade multiplier is 10x, chosen so 35 damage
            clears a 150 HP wall in exactly one hit - at two hits the phase
            stops reading as "walls no longer help", which is the entire point
            of the design. Boss health bar is polled rather than event-driven,
            because the boss is pooled and a bar lingering after death is
            worse than one frame of latency appearing.
2026-09-08  Deployables in, and the placer generalised. Explosive barrels chain
            a frame apart rather than instantly - it prevents unbounded
            recursion in one frame and reads better, since the chain visibly
            travels. Barrels hurt the player, which is what makes placing one
            a bet rather than free damage. Claymores are directional and
            one-use: walls choose where the horde walks, claymores punish the
            lane the walls chose. BarricadePlacer became DeployablePlacer
            driven by a catalogue of DeployableDefinition assets, so a fourth
            deployable is an asset and a button rather than a third
            near-identical script. Also fixed an input conflict introduced
            with endless mode - E was bound to both deploy and continue.
2026-09-08  Arena enlarged from 60x60 to 90x90, cover from 6 blocks to 14.
            Fixed a latent coupling in the process: the placement bound was a
            hardcoded copy of the old arena size, so resizing would have left
            a 90x90 map buildable only in its middle 54x54, with no error
            explaining why placement failed near the walls.
2026-09-08  Phase order changed again: no art at all until the content exists.
            The light art pass merged into Phase 3. Low risk here specifically
            because everything is builder-generated primitives - a late
            palette or silhouette change is a few lines in ArenaBuilder, not a
            re-authoring pass. The usual "you will build it twice" warning
            applies to projects with hand-made art; this one can absorb it.
2026-09-08  Phase order changed. Content production moves ahead of both the
            art pass and the Steam page. Two reasons it is sound: Phase 2
            retires ArenaBuilder, and content production is precisely the work
            that needs the builder alive - twelve enemies, ten weapons and
            several arenas wired automatically rather than by hand; and art
            direction is easier to lock once the content that must look
            consistent exists. A light art pass still runs first - post
            processing, lighting, VFX vocabulary - so content is built against
            a locked look rather than needing a second pass.
            The store page moving to month 8+ is a deliberate trade, recorded
            in Phase 5. Wishlists compound, so this gives up roughly four
            months of accumulation and lengthens the schedule from 56 to 64
            weeks, because the page and the demo can no longer overlap
            content. The upside is a page that launches showing a complete
            game. Flagged as the thing to revisit if Gate 5 wishlist growth
            looks weak.
2026-09-08  Boss played through. The fight runs as expected end to end - all
            three low-confidence numbers (2000 HP, whether phase 1 registers
            during a horde fight, and the un-telegraphed artillery) held up
            well enough not to need tuning before moving on.
2026-09-08  GATE 1 PASSED. Core mechanics validated across repeated runs and
            the hook is stateable. Noted honestly: gameplay and game design
            still want refinement. That is the correct outcome for this gate -
            it asks whether the design is worth building on, not whether it is
            finished. Phase 1 is otherwise complete except the boss and
            endless mode.
2026-09-08  Full 15-wave run played end to end. The arc holds up, so 20-25
            minutes is the length the boss gets designed against, and the
            gold economy and archetype ramp both survived the wave cap
            without needing retuning. Gate 1 is now runnable - until a run
            had an ending, five runs back to back could not measure anything.
2026-09-08  Wave cap and win screen in. WaveManager stops at wave 15 and calls
            GameManager.Win() rather than opening another Armory break -
            clearing the final wave is the win, so there is nothing left to
            shop for. GameState.Victory needed almost no wiring: every system
            already gated on State != Playing, so weapons, zombie AI, the wave
            loop, score, gold and barricade placement all stop for free. All
            nine gate sites verified. HUD wave label now reads WAVE n / 15.
            Compiles clean, not yet played end to end.
2026-09-08  Run structure decided: 15 waves into a boss, gold resets each run,
            winning unlocks endless. Boss chosen over a hold-out evac with the
            scope understood - it is a new enemy with telegraphed attacks and
            phases, which is Phase 4 content work pulled forward. The design
            risk to watch is a boss that is only a larger Brute: it has to
            stress the fortification hook, not just have more health.
2026-09-08  Destructible Wooden Barricades in (Boxhead DNA). Players purchase
            barricades at the Armory ($40) and place them via [F] or Right-Click
            with 1.0m grid snapping and 90-degree [R] rotation. Barricades feature
            150 HP, floating world-space health bars, friendly-fire immunity from
            player bullets (with piercing rounds punching through), and wood
            splinter destruction. Zombies detect blocking barricades and claw them
            down, resolving Tech Debt #6.
2026-09-09  Weapon upgrade trees in, Bloons-style. Three paths per weapon, five
            tiers each, bought with gold, restricted 5-3-0: one path to tier 5,
            a second to tier 3, the third locked out entirely. Weapons only -
            no player or economy trees. WeaponUpgradeTree/UpgradeTier are data,
            UpgradeManager owns purchases and enforces the rule against the
            configuration a purchase WOULD produce rather than the one it
            starts from, and resolves stats once per purchase into a cached
            struct so the firing path never walks tiers per shot.
2026-09-09  Upgrade panel in. Its one real design decision: "locked by 5-3-0"
            and "you cannot afford this yet" are drawn differently. Greying
            both identically makes the restriction read as arbitrary rather
            than as a consequence of a choice already made.
2026-09-09  Pistol tree designed three times, and the churn is worth recording.
            First draft was stat curves - damage up, fire rate up, money up -
            and was correctly rejected as three spreadsheets. Second draft
            (akimbo / ricochet chains / marks-and-detonate) was built and
            compiling before being replaced by the owner's own design; the
            ricochet and mark code was deleted rather than left dormant. Third
            and current: a single Gunslinger path, authored, with two columns
            left as placeholders for paths still to be designed.
2026-09-11  Four arenas, chosen per run. The layout was hardcoded inline in
            BuildEnvironment; it is data now, and every layout is built into
            the scene with all but one switched off at runtime, because the
            builder generates the scene once and cannot know which arena a
            future run wants.
              The Yard        scattered blocks and long anchors - the balanced
                              baseline everything was tuned in
              The Corridors   long parallel lanes. Sightlines run one way and
                              not the other, so it is the sniper's and rifle's
                              arena and the flamethrower's worst. A barricade
                              across a lane closes it completely, which should
                              make fortification stronger here than anywhere.
              The Ring        open centre, dense band at mid radius, open edge.
                              Kiting is easy, holding a spot is hard, and
                              ranged enemies get clean shots across the middle.
              The Warren      26 small blocks, no long sightlines. The
                              shotgun's and flamethrower's arena, and the
                              hardest test the flow field gets - every route is
                              a sequence of corners.
            All 90x90 with a clear centre, so spawn radius, placement bounds
            and the player's start need no per-arena tuning. The difference is
            entirely in what the cover does.
            TWO ORDERING BUGS HAD TO BE FIXED FOR THIS TO WORK, and both were
            the silent kind. FlowField baked its static blocking in Awake and
            ArenaSelector picks in Awake; Unity guarantees nothing about
            Awake-versus-Awake order, so the field could have mapped the walls
            of an arena that was about to be switched off. The bake moved to
            Start, which is guaranteed to follow every Awake.
            And enemies spawn on a ring of radius 38 with no geometry check.
            The Corridors has slabs that ring crosses, so a zombie could be
            born inside one and have to shove its way out before it could
            chase. Spawn points are now rejected for being inside cover, with
            damageables ignored so a barricade or another zombie standing there
            is still fine. That was latent in The Yard too - the new layouts
            only made it likely.
2026-09-11  Weapon animations wired. Every weapon carries nine to sixteen
            authored clips; four of them are now driven from the weapon's own
            events, and the rest are honestly left inert.
              Fired      -> Fire, restarted from frame 0
              ReloadStarted / ReloadFinished -> Reload, then Idle
              weapon swap -> Equip, then settle to Idle
            WeaponAnimator lives on the PLAYER, not on each model, because what
            changes on a swap is which animator to talk to rather than what to
            say to it. WeaponVisuals owns the swap and hands over the new one.
            Fire is RESTARTED rather than cross-faded - each shot is its own
            recoil, and blending one into the next is exactly what makes a fast
            weapon look like it is idling. Both Fire and Reload are speed-fitted
            to the weapon's real numbers, because upgrades move both: a gun
            firing every 0.067s would otherwise only ever show the first frames
            of its clip, and a 1.9s reload clip would outlast a 0.45s reload.
            Clip lengths are measured off the controller at swap time rather
            than authored as constants, so a re-exported weapon with different
            timings needs no code change. The serialized durations remain as
            fallbacks only.
            Bolt, pump and slide clips play only when the interval actually
            fits the recoil AND the cycle. On an SMG the cycle would be cut off
            every shot and just add noise. The cycle clip is found by name at
            runtime, so a weapon gaining or losing one needs no change.
            NOT DRIVEN, because nothing in the game maps to them: Charge,
            Inspect, Melee and Unequip. Unequip in particular would have to
            play BEFORE a swap completes, which means holding the swap for its
            duration - a gameplay change rather than an animation one.
            Two bugs found doing it. ReloadFinished was raised only when a
            reload was CANCELLED, never when one completed, so anything
            listening would start a reload animation and never be told to stop.
            And ConfigureWeaponImportSettings had been written twice; the
            surviving version rebuilds from defaultClipAnimations so removed
            takes cannot survive in the .meta and desync a fresh checkout from
            the editor cache.
2026-09-11  Main character integrated, and split onto two animator layers.
            Model, controller and PlayerAnimator replace the greybox capsule.
            Nine clips driven from existing systems - movement from
            InputReader, fire and reload from Weapon, reactions from Health -
            with root motion off so the rig never fights the collider.
            THE TWO LAYERS ARE THE POINT. On one layer the firing clip wins and
            a twin-stick player, who moves and shoots simultaneously almost
            constantly, is never seen to walk. Base layer carries Idle, Walk,
            Run and Death full-body; a mask from the spine up carries Aim,
            Fire, Reload, GetShot and Stagger over the top. The legs answer to
            movement and are never locked.
            Pelvis and Root deliberately stay with locomotion. Masking those
            into the upper body leaves the legs swinging under a body that
            never shifts its weight.
            The mask is generated from the model's real bone paths and matches
            the Spine segment exactly, so a future rig with a Spine2 somewhere
            unrelated cannot quietly opt itself in.
            Three bugs the integration surfaced, all fixed:
              The player model floated a metre up - same cause as the zombie,
              parented at local zero while a CharacterController is centred on
              its pivot. The height is a named constant now, because the model
              has to be dropped by half of it BEFORE the controller exists, and
              having to read it back is what made this easy to get wrong twice.
              WeaponSocket_L was never mirrored. Both hand sockets sat at
              x +0.52, copy-pasted without flipping the sign, so akimbo would
              have put both pistols in the right hand. Fixed at source.
              The reload clip is 1.9s and was held at full length against
              reloads that upgrades cut to 0.45s, so the survivor kept fumbling
              for over a second after the weapon was loaded.
            And a flaw in that last fix, found only once there were two layers:
            it used Animator.speed, which is GLOBAL, so a 4x reload would have
            sprinted the legs too. Now a ReloadSpeed parameter bound to the
            Reload state alone. Worth remembering as a shape - a fix that is
            correct on one layer can be wrong the moment there are two.
            NOT PLAY-TESTED, and this is the first change whose correctness can
            only be judged by looking at it. The mask boundary at the spine is
            the thing most likely to need moving.
2026-09-11  Weapon models wired into the player's hands. Two holders replace
            the two primitive cubes; WeaponVisuals toggles which of ten models
            is active inside them and moves the muzzle and eject port onto that
            model's own sockets. Every weapon's flash, tracers and shells now
            come out of ITS OWN barrel rather than one fixed point at 1.35m -
            the pistol's muzzle moves back to about 1.0m and the sniper's out
            to about 2.0m.
            All ten models exist from the start and are toggled rather than
            spawned on swap: weapon switching happens mid-fight, and an
            Instantiate there would be a hitch for nothing. Each is positioned
            by its GripSocket rather than its mesh origin, which is what makes
            ten different lengths line up in the same hand.
            ONE FRAGILE SEAM, commented at both ends: the muzzle transform now
            has two owners writing DIFFERENT AXES. WeaponVisuals writes local Y
            and Z from the socket, Weapon writes local X for the akimbo offset.
            It works only because they never overlap - if either starts writing
            a full position the other silently loses.
            WeaponModelScale is exposed as one constant, currently 1.0. The
            authored sniper is 2.62m against a 2m player, so its muzzle sits
            nearly a body length ahead; that is the first thing to judge in
            scene rather than on paper.
2026-09-11  Fixed the normal zombie floating at waist height. The model is
            authored standing on zero, but a CharacterController is centred on
            its pivot, so parenting at local zero left every zombie hovering
            0.95m - half its controller height - off the ground. Caught by
            measuring the authored mesh rather than by playing, and it had been
            in the build since the model landed.
            Worth generalising before the player model goes in: any authored
            character parented to a controller needs a -height/2 offset, and
            the player will need -1.0.
2026-09-11  First authored character, and its cost brought down to fit a horde.
            The normal zombie shipped at 19,708 triangles and 10,112 vertices,
            on the BASELINE archetype - spawn weight 10, never falls off, so
            most of the sixty alive at wave 15 are this mesh. The camera sits
            23m out, making it about eighty pixels tall on a 1080p screen.
            Now 6,660 triangles and 3,588 vertices. Vertices are the figure
            that matters: skinning cost scales with them, so per-frame work
            for a full horde went from 607k to 215k.
            Lowered at GENERATION time rather than by decimating a finished
            mesh - primitives built coarse stay clean, decimated ones do not.
            build_zombie.py takes ZOMBIE_DETAIL=horde|hero, and call sites that
            ask for resolution positionally (the cranium asks for 24x16) are
            CLAMPED rather than edited, so authored intent stays readable and
            hero still produces exactly what was written.
            A Unity LODGroup would have achieved nothing: the top-down camera
            never changes distance, so every zombie would sit on the same LOD
            level forever. Worth remembering before reaching for LODs on any
            future character.
            ArenaBuilder.TuneForHorde does the rest, and it is worth more than
            the polygon count - skinned meshes do not batch and the GPU
            Resident Drawer does not touch them, so every horde member is its
            own draw call and its own skinning pass, and a shadow-casting one
            pays both twice. Shadow casting off, two-bone skinning, no
            per-frame bounds recalculation, and animator culling that keeps the
            state machine ticking while skipping transform writes nothing can
            see.
            Verified by rendering, not just by the counter: the silhouette,
            palette and expression survive, slightly more faceted.
            STILL NOT PROFILED IN PLAY. These are reductions in known costs,
            not a measurement.
2026-09-10  Weapon roster finished - 10 weapons, 10 paths.
              SMG "Adrenaline" (120) stacks move speed AND fire rate per kill,
              lapsing on a timer. Move speed was cut from the pistol path for
              reaching into PlayerController; here it is the weapon's entire
              identity, so PlayerController gained the same SpeedMultiplier
              ZombieAI already had. Stacks lapse on a TIMER rather than on
              standing still - a velocity check would punish backing off round
              a corner, which is the one thing the weapon wants encouraged.
              Nail Gun "Lawman" (180) is weak alone on purpose. Nails fired
              into your own barricade mend it, 22 rising to 70 a hit; T2 pins;
              T5 mounts a turret on every wall you place. The turret sits
              dormant on the barricade prefab and switches ITSELF on while a
              carried weapon grants it, so the placer never learns about
              upgrades.
              Siphon Rifle "Reaper" (380) is the only healing in the game.
              Damage scales with health LOST, so it pays out exactly when the
              lifesteal is most needed, and T5 refuses one killing blow a wave.
              That reuses IDeathInterceptor, MOVED from ZombieAI into Health so
              the player gets it too - the right home, since no death ever
              happens and the run-over screen, score tally and wave loop never
              see it.
            Two defects caught in review. The Nail Gun was authored as a
            projectile weapon, which made its entire path inert:
            WeaponProjectile carries its own damage and never calls back into
            Weapon.ApplyShot, where repairing and pinning live. That is debt
            20, written down two sessions earlier and still nearly missed - it
            is hitscan now, with the reason in a comment rather than left to be
            rediscovered. And the turret resolved ten carried weapons per
            barricade per frame just to learn it had no turret, a cost paid
            entirely by players who never bought the tier.
            NOT PLAY-TESTED.
2026-09-10  Enemy roster finished - 12 archetypes plus the boss. The last five
            each attack a different assumption rather than varying a stat:
              Leaper (5+) vaults barricades on a sine arc with a 2.5s
              cooldown, so it clears A wall rather than a wall line. Needed a
              new IEnemyMotionOverride hook - anything leaving the ground has
              to drive its own movement, because the ordinary path pins the
              controller down every frame.
              Screamer (6+) drives every zombie within 12m at 1.5x speed while
              it lives. Tall, thin and brightly capped on purpose: "kill that
              one first" is not a decision the player can act on if they
              cannot pick it out of a crowd. The buff is refreshed rather than
              tracked, so anything leaving the radius simply stops being
              refreshed - a dead screamer needs no cleanup pass and cannot
              leave the horde permanently enraged.
              Revenant (7+) stands back up once at 45% health. Needed
              IDeathInterceptor, asked BEFORE the death sequence: reviving
              afterwards means unpicking a score payout, a gold payout, a
              disabled collider and a queued despawn. Keeps a magenta tint for
              the rest of its life so the player can tell which bodies still
              owe a second kill.
              Spitter (7+) reuses the ranged behaviour wholesale - the only
              difference is that its projectile leaves a 2.6m acid pool for
              six seconds. That is the answer to camping, which nothing in the
              horde previously had.
              Crawler (3+) is 22 HP at 5.4 speed and arrives FOUR at a time.
              Needed groupSize on the spawn table, and the spawn loop now
              counts bodies rather than iterations, clamping a group to the
              wave's remaining budget so a late cluster cannot overshoot the
              count the HUD already announced.
            Two bugs caught before they shipped. The Spitter would never have
            fired: attackRange gates the RANGED attack too, and it had 1.5m
            while being told to stand at 15m - it would have walked to range
            and stared. And a projectileDamage field was set that does not
            exist; Fields silently ignores unknown names, so it would have
            compiled, built and quietly used the wrong number. See debt 24.
            Cost of the whole roster, architecturally: three hooks (motion,
            death interception, speed multiplier), groupSize, and an
            impact-spawn field on EnemyProjectile. Every archetype is now a
            prefab and a line in the spawn table, which is what doing the
            table first bought.
            NOT PLAY-TESTED. Screamer and Crawler together could be genuinely
            nasty, and the Leaper is the most likely to have arc bugs.
2026-09-10  Enemy archetypes 4 to 7, and the wave roster stopped being
            hardcoded. WaveManager held four prefab fields, three start-wave
            ints and a weight formula with a branch per type; twelve
            archetypes do not fit that. It is now a spawn table of
            {prefab, startWave, weightAtStart, growthPerWave, cap}, so a
            twelfth archetype is a line in an array and a prefab - no field,
            no branch. The first four entries reproduce the old formula
            exactly, so pacing that was already play-tested is unchanged.
            The three new ones were chosen so each needs a DIFFERENT kind of
            hook, giving the remaining five a pattern to slot into instead of
            each inventing one:
              Bloater (wave 5+, 180 HP) bursts for 34 in 4.5m when killed.
              Hooks Health.Died. Deliberately does not damage other zombies -
              one that cleared the pack around it would reward killing it in a
              crowd, the opposite of the pressure it exists to apply.
              Armored (wave 6+) takes 25% damage inside a 70-degree frontal
              arc. Needed a new IDamageModifier hook, queried by Health BEFORE
              damage lands, because the Damaged event fires afterwards and
              cannot change a number. Carries a visible plate so "shoot it
              from the side" is readable rather than inferred. Blasts ignore
              the arc entirely, which is the intended counterplay.
              Sapper (wave 4+) walks at the nearest barricade instead of the
              player and does 4x damage to it. Needed an IEnemySteerOverride
              hook that changes STEERING ONLY - attacks still resolve against
              whatever it is pressed against, so the existing blocking check
              does the damage and there is no second attack path to keep in
              step with the first.
            Also fixed while in there: Validate still looked for the four
            deleted prefab properties and would have reported a builder bug on
            every single run. It now checks every spawn-table entry rather
            than just the first, because one silently null archetype otherwise
            looks like bad luck with the weights.
            NOT PLAY-TESTED. The Sapper is the one most likely to be wrong:
            if barricades evaporate the moment they are placed, drop the 4x or
            push its start wave later.
2026-09-10  One weapon per firing mode, so cone, chain and projectile finally
            execute. Flamethrower (250) is 7 damage ten times a second in a
            26-degree cone at 11m off a 100-unit fuel tank; Tesla Coil (300)
            is 34 damage hopping to three more bodies at 8m; Grenade Launcher
            (280) lobs 110 over a 4.5m blast, four shells at a time.
            Their paths forced a real improvement. Delivery parameters lived
            in the delivery ASSET, so a Wildfire path could not widen its cone
            and a Storm path could not grow its chain - the two things those
            fantasies are entirely about. WeaponStats now carries
            ConeHalfAngle, ChainBounces, ChainHopRange, ChainDamagePerHop,
            BlastRadius and Range, all -1 meaning "use the delivery's own
            value". The asset says where a weapon starts, the path says where
            it goes.
            Six more fields, and again every one is a plain override of a
            parameter that already existed rather than a new mechanic - the
            same pattern as the shotgun/rifle/sniper batch. See debt 17: the
            vocabulary keeps growing in the least interesting direction, which
            is a strong hint about what the eventual rework should target.
            It paid off immediately: Bombardier's Cluster tier is just
            pelletCount = 3, because ProjectileDelivery already loops the
            pellet count. Three shells a trigger pull cost no code at all.
            NOT PLAY-TESTED, and these are the first things ever to run the
            three new modes - expect mode bugs before balance problems.
            Storm's T5 gains 10% per hop over eight hops, which is the same
            compounding shape as sniper overpenetration and the same first
            candidate for tuning.
2026-09-09  Horde pathing fixed. Zombies steered in a straight line at the
            player with no awareness of geometry, so anything behind a cover
            block pressed into it forever, held there by its own
            CharacterController. Replaced with a shared FlowField: one
            Dijkstra sweep over a 90x90 grid at 1m cells - the grid barricades
            already snap to - rebuilt only when the player crosses into
            another cell or the barricades change. Each zombie reads the cell
            it stands in, which is one array lookup and cheaper than the
            normalize it replaced.
            NOT NavMesh, deliberately: the scene is builder-generated so
            baking is awkward, runtime barricades would each need a carving
            obstacle, NavMeshAgent wants to own transform movement and would
            fight the CharacterController and the knockback impulse, and sixty
            agents of NavMesh avoidance is heavier than a field lookup.
            Barricades are stamped EXPENSIVE rather than impassable, at cost
            30. Impassable would have made the horde walk around a wall
            instead of clawing through it, which quietly deletes the
            fortification hook. At this cost a short wall is a detour and a
            long one is a door. That number is the first thing to tune:
            zombies pressing on a wall they could route around means it is too
            low, absurd detours around two panels means too high.
            Within 3 metres zombies revert to a straight line - the grid is
            coarse next to the player, the attack range wants precision, and
            any failure of the field degrades to the old behaviour rather than
            to standing still.
            Three defects caught in review, two of them the quiet kind. The
            bake used CheckBox, which does not care what it hits: the player
            is standing on the grid when it bakes, so their spawn cell would
            have been permanently impassable for the whole run. The heap was
            sized at 4x the cell count when lazy deletion makes the true bound
            8x, so a full heap would have silently dropped pushes and produced
            a quietly WRONG field rather than a crash. And queries ran before
            Physics.SyncTransforms, on the frame the scene loads.
2026-09-09  Firing modes built, closing debt 19 and unblocking the six
            remaining weapons. WeaponDelivery is a ScriptableObject with one
            method rather than an enum with a branch per mode - the same
            reasoning that turned deployables into definitions and got mod
            cores deleted. An enum would have meant every new weapon kind
            editing the same file, which is the debt this project keeps
            paying off.
            The split is the part worth remembering: Weapon keeps everything
            true of every firearm (ammo, rate, which hand fired, whether the
            shot crit, which direction the ultimate picked, plus focus,
            executions and kill credit), and a delivery owns only how the shot
            travels. Everything a delivery needs arrives in a ShotContext
            rather than being reached for through the weapon, so a delivery
            can be read without reading Weapon.cs - and akimbo, the ultimate,
            crits and focus work with every mode for free.
            Four modes: Hitscan (a straight port, no behaviour change),
            Cone (flamethrower - continuous fire modelled as a very high rate
            of very small shots, so a fuel tank is a magazine and a tick of
            flame is a round, rather than a second firing lifecycle running
            beside the first), Chain (Tesla - first link is a real raycast so
            walls block it and aiming matters, every hop after is a search
            because an arc refusing to bend around a corpse would read as
            broken), and Projectile (grenade and nail, spherecast swept so a
            fast round cannot tunnel, statically pooled like EnemyProjectile).
            Blast is back from the marks path, better: enemies from the horde
            list, plus a small physics query purely to chain explosive
            barrels, and it touches neither the player nor their barricades.
            NONE OF THE THREE NEW MODES HAS EVER RUN. No weapon references
            them yet, so the first weapon built on each mode is really a test
            of that mode.
            One deliberate behaviour change: auto-aimed shots (the ultimate,
            Split Focus) used to fire exactly one pellet and now fire the
            weapon's full count. No live weapon hits this - only the pistol
            has either - but the old behaviour was wrong.
2026-09-09  Design direction changed, and this is the third time the upgrade
            structure has moved, so the reasoning matters more than the result.
            Three paths per weapon on four weapons meant 60 tiers, and the
            5-3-0 rule needed all three to exist before any of them was a
            choice. Settled instead on TEN weapons with ONE path each: the run
            decides which weapon to pour gold into, and with a maxed path at
            3360 against 6580 income it can only ever afford one. The player
            handles all ten guns every run; exactly one of them feels
            powerful. Cost is the same 50 tiers, but the variety now comes
            from the arsenal rather than from branching inside one gun.
            5-3-0 survives untouched in UpgradeManager and simply goes inert -
            a single open path can never breach it - so a weapon can be given
            three paths later as data plus a wider panel.
2026-09-09  Loadout reworked, closing debt 16. WeaponLoadout now holds a
            catalogue (everything the Armory sells) and a carried array (what
            the run holds), replacing a fixed slots[4] with a parallel
            unlocked[4] that could not express ten. Carry capacity is 10,
            mapped to the number row 1-9 then 0. Weapon prices moved onto
            WeaponDefinition, so ArmoryPrices is down to a single field and a
            tenth weapon is an asset plus a catalogue entry - no constant, no
            branch, no button. The shop builds its weapon list in a loop and
            sizes its own card to fit it.
2026-09-09  Three more upgrade paths, all hitscan so none needed new firing
            code. Shotgun "Streetsweeper" ends as a 12-pellet 200 RPM wall
            that feeds itself a shell per kill. Assault rifle "Marksman"
            stacks damage for consecutive hits on the SAME target and tightens
            its spread the longer you hold - the deliberate mirror of the
            pistol's fan, both driven off one triggerHeat value in opposite
            directions. Sniper "Executioner" makes each pierced body make the
            round stronger rather than weaker, and executes anything under 30%
            health whatever its maximum, which is the answer to the boss.
            Numbers to watch: overpenetration compounds to roughly 9x on the
            twelfth body, and the sniper execute deletes the last 600 HP of
            the boss for free.
2026-09-09  Economy measured and repriced. A 15-wave run earns 6580 gold; the
            most all four weapons could absorb under the old flat tier curve
            was 6870, so a run could buy 96% of everything the game sells and
            no purchase was a real choice. Tier costs are now 60/150/350/800/
            2000 - one maxed path is 3360, most of a run - defined once in
            ArenaBuilder.TierCosts and applied by a helper so sixty tiers
            across twelve paths cannot drift out of step. Also fixes pacing
            for free: 55% of a run's gold arrives in the last five waves, and
            the expensive tiers are now what that money is for.
2026-09-09  Mod cores deleted - all five, plus ModCoreType, their prices, the
            shop column and WeaponDefinition's overclock fields. Closes debt
            15 by deletion rather than migration, and removes the untested
            stacking between mods and upgrade trees. Double tap removed from
            the Gunslinger T2 as well. Fixed while in there: the HUD never
            showed an infinite reserve, because it only checked the weapon
            definition and not the resolved upgrade stat; and the shop's
            deployable buttons overlapped the ammo and barricade rows by 21px,
            a layout bug that predated all of this.
2026-09-09  Decision: three upgrade paths on all four weapons, not one. Sixty
            tiers total. Taken with the content cost understood, on the basis
            that the economy rework is what makes any of them a choice.
2026-09-09  Gunslinger path live. T1 guaranteed crit on the first shot after
            every reload; T2 25% crit and double tap; T3 fan the hammer, hold
            to fire at 600 RPM with spread bleeding 1.2 to 9 degrees and
            recovering on release, while a click stays an accurate double tap;
            T4 dual pistols, 45 damage, infinite reserve; T5 an ultimate at 30
            pistol kills - flourish reload, then the guns auto-aim while the
            player spins, 80% crits until the magazine runs dry.
            Two things checked rather than assumed: reload-spamming for the
            free crit is worse DPS than simply shooting, at every tier; and
            the guaranteed crit only arms on a reload that actually loaded
            rounds, so a dry reserve cannot hand one out.
            NOT PLAYED YET. Numbers most likely to need tuning are the 0.2s
            hold threshold that separates a tap from a fan, and the ultimate's
            duration - 30 rounds at 900 RPM is 2.0 seconds, which may be short
            for something that costs 30 kills.


===============================================================================
PHASE 0 - PROVE IT'S FUN WITH ZERO ART            Weeks 1-2
===============================================================================

[x] Tuning pass - DONE 2026-09-08. Movement, shooting, aiming, reloading all
    confirmed good in play. No adjustments needed.
[x] Enemies spawning and killable - DONE 2026-09-08. Zombies spawn, take
    damage, die, and recycle. Wave counter and score both advance.
[x] Hit feedback - DONE 2026-09-08. White flash on every hit, knockback along
    the bullet path, 50ms hit-stop gated to kills and behind a toggle.
[x] Weapon feedback - DONE 2026-09-08. Muzzle flash with a real light pop,
    impact sparks, tumbling brass that bounces off the floor, and a directional
    camera recoil kick that springs back.
[x] Screen shake - DONE 2026-09-08. Trauma-based, squared response curve, on
    firing, kills and taking damage. Perlin noise rather than random jitter.
[x] Death moments - DONE 2026-09-08. Body squashes, spreads and spins out over
    0.2s. Pop chosen over dissolve or gibs as the only zero-asset option.
[x] First audio - DONE 2026-09-08. Gunshot, impact, death and a looping low
    ambient bed, all synthesized placeholders in Assets/_Project/Audio.

GATE 0: PASSED 2026-09-08. Run with an outside player, good feedback.


===============================================================================
PHASE 1 - FIND THE HOOK                           Weeks 3-8
===============================================================================

[x] Move weapon stats into ScriptableObjects - DONE 2026-09-08.
    WeaponDefinition holds every stat; Weapon executes one against scene
    wiring. Assets live in Assets/_Project/Weapons.
[x] Build 3-4 weapons that play differently - DONE 2026-09-08. Pistol,
    shotgun, assault rifle and sniper, switchable on 1-4. They differ in fire
    mode, penetration, knockback and range, not just in numbers.
[x] Enemy archetypes - DONE 2026-09-08. Standard, brute, runner and ranged.
    Each forces a different player response and validates weapon roles.
[x] Gold Economy & HUD - DONE 2026-09-08. Gold awarded on zombie kills scaled
    by archetype (10g/15g/25g/40g), displayed in HUD, and banked into run wallet.
[x] Inter-wave Armory & Shop - DONE 2026-09-08. Interactive pause/break shop to
    buy weapons, ammo restocks, and tangible weapon mod cores (dragon's breath,
    heavy slug, bore rounds, drum mags, overclocked receiver).
[x] Tactical Deployables (Wooden Barricades) - DONE 2026-09-08.
    Placeable wooden barricades (destructible 150 HP obstacles zombies claw
    through or path around). Red explosive barrels queued for next iteration.
[x] Define run structure - DECIDED 2026-09-08. 15 waves of escalation, then a
    boss encounter as the climax. Gold resets fully every run - meta
    progression stays a Phase 4 problem. Beating the boss unlocks endless
    survival. At current pacing that is ~337 zombies and 20-25 minutes, close
    enough to the Phase 5 demo length to reuse.
[x] Implement the run structure - BUILT 2026-09-08.
    DONE: wave cap at 15, GameState.Victory, win screen, and a WAVE n / 15
    counter so the run reads as an arc rather than a treadmill. Played end to
    end and validated - the 20-25 minute arc holds up, so that is the length
    the boss gets designed against.
    DONE: endless mode - [E] on the victory screen drops the wave cap and
    resumes the same run at wave 16, carrying health, gold, weapons and mods.
    DONE: the boss. Arrives 40% into wave 15 alongside the horde. Two phases -
    artillery at range, then an enraged charger at 50% health that smashes
    barricades in a single blow. Killing it is the win; clearing the wave is
    no longer the trigger. Played through 2026-09-08 and ran as expected.
    NOTE: endless is unlocked only within the session that won it. Making the
    unlock persist needs the save system, which is debt item 4, due Phase 3.
[x] Write the hook in one sentence - DONE 2026-09-08: "A top-down wave survival
    shooter where you spend blood money to physically engineer fortified
    killboxes and mod firearms into crowd-shredding weapons of war."

GATE 1: PASSED 2026-09-08. The core mechanics hold up across repeated runs and
the hook is stateable. Gameplay and game design still want refinement - but
refinement, not redesign, which is exactly what passing this gate means.
Everything from here is allowed to be polish and content.


===============================================================================
PHASE 2 - CONTENT PRODUCTION                      Weeks 9-28
===============================================================================

Reordered again 2026-09-08: no art at all until the game has its content. The
light art pass that briefly sat here has merged into Phase 3. This is lower
risk than it sounds - everything is builder-generated primitives, so a late
palette or silhouette change is a few lines in ArenaBuilder rather than a
re-authoring pass. ArenaBuilder stays alive for this entire phase; that is the
main reason content comes first.

[x] Bigger arena - DONE 2026-09-08. 60x60 to 90x90, 2.25x the area, cover
    from 6 blocks to 14 including long slabs for barricade lines to anchor
    against. Fixed a latent coupling while doing it: BarricadePlacer had the
    old arena bound hardcoded, so a resize would have silently confined
    building to the middle of the new map with no error.
[x] Deployables - DONE 2026-09-08. Explosive barrels and claymores, and the
    placer generalised so a fourth deployable is an asset plus a button, not
    another script. Barrels chain and hurt the player; claymores are
    directional and one-use.
[x] Enemy archetypes - DONE 2026-09-10. Twelve plus the boss: zombie, brute,
    runner, ranged, sapper, bloater, armored, leaper, screamer, revenant,
    spitter, crawler. Each attacks a different assumption rather than varying
    a stat - walls are not safety (leaper), walls are the objective (sapper),
    close range is dangerous (bloater), facing matters (armored), one target
    matters more than the nearest (screamer), a body is not finished (revenant),
    standing still is not free (spitter), and a pack is not a stream (crawler).
    Roster lives in WaveManager's spawn table; a thirteenth is a prefab and a
    line. Numbers are entirely unverified.
[x] Weapons - DONE 2026-09-10. Ten, each with one five-tier path: pistol,
    shotgun, rifle, sniper, flamethrower, Tesla coil, grenade launcher, SMG,
    nail gun, siphon rifle. An eleventh is a definition asset, a catalogue
    entry and a path helper.
    UNBLOCKED as of the delivery rework - all four firing modes exist, so each
    remaining weapon is a definition asset, a catalogue entry and a path
    helper. The Nail Gun's pinning is an upgrade behaviour for its path, not
    delivery infrastructure, so that one still needs code.
[~] Weapon upgrades. Trees are in and are data, not branches: ONE path of five
    tiers per weapon, bought with gold. Costs come from one shared curve
    (ArenaBuilder.TierCosts), so authoring a path is choosing five effects,
    not five effects and five prices.
    PROGRESS: 10 of 10 paths authored - Gunslinger, Streetsweeper, Marksman,
    Executioner, Wildfire, Storm, Bombardier, Adrenaline, Lawman, Reaper.
    Mod cores are gone entirely, which closed debt item 15 by deletion.
    Remaining constraint is debt item 17, and the three new paths cost seven
    more fields exactly as predicted.
[x] Arenas - DONE 2026-09-11. Four with genuinely different character: The
    Yard, The Corridors, The Ring, The Warren. Chosen at random per run by
    ArenaSelector; ArenaLayout is data, so a fifth is a line in an array.
    Unverified: whether The Corridors really does make barricades stronger,
    and whether The Warren's seeded scatter produces an awkward clump - it is
    deterministic, so it would be the same clump every build.
[ ] Meta progression - needs the save system, debt item 4.
[ ] Enemy models. Only the normal zombie is authored; eleven archetypes and the
    boss are still primitives. Twelve archetypes that look like three variations
    of a capsule READ as repetition even when they are not, so this is a Gate 2
    item rather than a Phase 3 one.
    Priority is by how load-bearing the silhouette is: Screamer first, since
    its entire design is "pick it out of a crowd", then Bloater and Armored,
    whose counterplay the player cannot infer from damage alone.
[ ] Performance - profile the horde. Debt item 6, deferred to exactly here.

GATE 2: 10+ hours of play without repetition becoming obvious, and no run
feels like a strictly worse version of another.


===============================================================================
PHASE 3 - ART DIRECTION & VERTICAL SLICE          Weeks 28-36
===============================================================================

All art now happens here, cheap half and expensive half together, once every
piece of content that has to look consistent exists.

[ ] URP post-processing stack - bloom, color grading, vignette, subtle grain.
    Does more for a minimal look than any model would.
[ ] Lighting design - committed palette and mood, strong silhouettes,
    high contrast.
[ ] VFX language - consistent rules for muzzles, impacts, deaths, pickups.
[x] Main character integration - DONE 2026-09-11. Model, controller,
    PlayerAnimator, and a masked upper-body layer so the legs can run while the
    arms shoot. The socket mirroring bug is fixed at source.
[ ] Weapon clips with no gameplay behind them. Charge, Inspect, Melee and
    Unequip are authored on all ten weapons and driven by nothing. Either give
    them a mechanic - a melee shove has obvious value when something is in your
    face - or accept they are unused and stop exporting them.
[ ] Decide where the weapon hangs. The holders still sit on the player root at
    fixed offsets, so the gun does not follow the hand through Aim, Fire or
    Reload. WeaponSocket_R exists and would look far better.
    The reason this is a DECISION rather than a task: parented to the hand, the
    ANIMATION points the barrel rather than the aim. In a twin-stick the gun
    direction IS the aim, and Weapon casts from the player's centre along the
    muzzle's forward - so hand-parenting means either accepting that shots no
    longer visually leave the barrel, or driving the arm procedurally to face
    the cursor. The second is the real answer and it is IK work, not parenting.
[ ] Occlusion fade - the player must stay visible behind cover, ARPG style.
    Deferred here from Phase 2 on 2026-09-09, deliberately: it is a rendering
    and art decision, it belongs beside the post-processing and lighting work
    above, and the shader wants writing against the final materials rather
    than against greybox primitives that are about to be replaced.
    Three levels, and the middle one is the target: fade the whole occluder
    (cheap, no shader, but a wall vanishing pops badly); a soft screen-space
    circular cutout that dithers away only near the player (what ARPGs
    actually do); and a ZTest Greater silhouette pass so the character is
    never lost even when the cutout misses.
    WARNING that will otherwise cost a day: this project runs the GPU
    Resident Drawer, which ignores MaterialPropertyBlock - the reason HitFlash
    uses per-instance materials. The cutout and the silhouette are fine
    because they are shader-level; the cheap fade would need per-instance
    material swapping, which is a second reason not to bother with it.
[ ] Arena as level design - replace the generated box with hand-authored
    spaces: cover, chokepoints, sightlines, a reason to move.
[ ] RETIRE ArenaBuilder here (debt item 1). One-way door: after this, every
    new prefab and its wiring is manual scene work.
[ ] UI pass - migrate to TextMeshPro, design real HUD, main menu, pause.
[ ] Audio pass - full mix, music bed, dynamic intensity between waves.
[ ] Migrate InputReader to Input Actions + rebinding UI (debt item 2).

GATE 3: Capture 60 seconds of unedited play you would put on a store page
as-is. That clip becomes the announce trailer.


===============================================================================
PHASE 4 - STEAM PAGE LIVE                         Weeks 36-40
===============================================================================

Moved here from month 4 on 2026-09-08, deliberately. The trade, recorded so it
stays a decision rather than an accident: wishlists compound, so a page live
four months earlier reliably outperforms a better-looking page later. This
route gives up roughly four months of accumulation and lengthens the schedule,
because the page and demo can no longer overlap content production. The upside
is a page that launches showing a complete game. Revisit if wishlist growth
looks weak at Gate 4.

[ ] Store page - capsule art, screenshots, description, tags.
    Capsule art is the click-through gate; weight it accordingly.
[ ] Announce trailer cut from the Gate 3 capture.
[ ] Steamworks setup - App ID, depots, build upload pipeline,
    achievements, Steam Cloud.
[ ] Start devlog cadence - later than ideal, so lean on the content backlog
    for material.

GATE 4: Page live and accumulating wishlists on its own.


===============================================================================
PHASE 5 - DEMO & NEXT FEST                        Weeks 40-48
===============================================================================

[ ] Demo build - first 20 minutes, self-contained, ending on a hook.
[ ] Telemetry - where players quit, which upgrades get picked, which weapons
    never do, where they die.
[ ] External playtests in rounds. Watch silently. Do not explain controls.
[ ] Onboarding pass - most first-time-player problems surface here.
[ ] Register for Next Fest deliberately - one-shot visibility spike, align it
    to a strong build.

GATE 5: Demo retention holds past 10 minutes for most players, and the
wishlist curve visibly steps up during the fest.


===============================================================================
PHASE 6 - LAUNCH PREPARATION                      Weeks 48-64
===============================================================================

[ ] CONTENT FREEZE at week 52. Bugs and balance only after that.
    Anything new becomes post-launch update material.
[ ] Steam Deck verification.
[ ] Accessibility & settings - colorblind-safe palette, difficulty options,
    screen shake toggle. Rebinding already landed in Phase 3.
[ ] Localization for highest-value languages, if text budget allows.
[ ] Launch trailer, press & streamer outreach, keys out several weeks ahead.
[ ] Confirm pricing and release date.

GATE 6: Release candidate runs 2 weeks with no new critical bugs, on both
desktop and Steam Deck. Ship it.


===============================================================================
CONTINUOUS TRACKS - run alongside every phase
===============================================================================

[ ] Playtesting        WEEKLY   Someone other than you plays, from Phase 0 on.
                                Watch without commentary.
[ ] Audience building  2-3/WK   Short clips of game feel and VFX work.
                                Start now, not at the store page.
[ ] Build hygiene      ONGOING  Keep repo clean, tag milestone builds, make a
                                runnable build weekly.
[ ] Runway tracking    MONTHLY  Know your months of runway vs. current phase.


===============================================================================
KNOWN DEBT IN THE CURRENT BUILD
===============================================================================

1. ARENA SCENE IS GENERATED.
   ArenaBuilder overwrites Arena.unity wholesale. Retire it the moment
   hand-authoring starts - now Phase 3, not Phase 2 - or it will silently
   destroy level design work. Keeping it alive through content production is
   deliberate: it wires every new enemy, weapon and arena automatically.

2. INPUT IS POLLED DIRECTLY.
   InputReader reads devices rather than action maps. Commercial release needs
   full rebinding. Migrate to Input Actions in Phase 3 with the UI pass.

3. HUD USES LEGACY uGUI TEXT.
   Chosen to avoid the TextMeshPro import prompt on first run.
   Migrate in the Phase 3 UI pass.

4. NO SAVE OR SETTINGS PERSISTENCE.
   Needed for Phase 2 meta progression, and for making the endless-mode unlock
   persist. Expensive to retrofit, and it now arrives sooner than planned
   because content moved earlier.

5. NO ASSEMBLY DEFINITIONS. - DEFERRED, deliberately.
   Everything compiles into Assembly-CSharp. Reviewed 2026-09-08: 31 scripts
   do not cost real minutes yet, and asmdefs carry their own setup risk around
   editor and package references. Revisit when a compile is actually annoying.

6. ZOMBIE STEERING IS O(n^2), NOW WITH A PER-FRAME SPHERECAST ON TOP.
   ZombieAI.Separation walks every other live zombie each frame
   (ZombieAI.cs:210). Barricade sensing then adds a SphereCastNonAlloc per
   zombie per frame (ZombieAI.cs:141). At 60 agents that is ~3,600 distance
   checks plus 60 casts every frame; fine today, and the first thing to break
   when waves get dense.
   NOTE: this entry was briefly overwritten by the barricade-pathing note and
   marked resolved. It was not resolved - it got worse. Phase 4 references it
   for the spatial hash, so keep it at number 6.
   Fix: spatial hash or grid buckets for separation, and cache the barricade
   cast to every few frames rather than every frame.
   UNCHANGED by the flow field (2026-09-09). The field replaced the chase
   direction only; separation still walks every pair and the barricade cast
   still runs per zombie per frame. It did not get worse either - a field
   lookup is cheaper than the normalize it replaced.
   RENDER COST ADDRESSED SEPARATELY 2026-09-11, and the two should not be
   confused. Authored characters added a per-frame render and skinning cost
   the greybox capsules never had; that has been cut roughly threefold and is
   documented in the art README. THIS entry is still about CPU steering work,
   which nothing has touched. When the horde is finally profiled, expect to
   find out which of the two actually dominates - the honest answer today is
   that nobody knows.
   DEFERRED, deliberately. Reviewed 2026-09-08: maxAliveAtOnce is 60, so this
   is ~3,600 distance checks and 60 casts per frame - trivial for a modern
   CPU. A spatial hash now is premature optimisation. Revisit in Phase 2 when
   content production raises horde counts - that phase moved earlier, so this
   arrives sooner than originally scheduled.

7. SCENE SINGLETONS FAIL SILENTLY. - RESOLVED 2026-09-08.
   SystemsCheck runs in Start, after every Awake has assigned its instance,
   and reports any missing singleton as one Console error naming exactly what
   is absent and how to fix it. The null-conditional call sites stay - they
   are correct - but the whole class of failure is now loud instead of silent.
   Original entry follows.
   ---
   Reached through null-conditional calls, so one missing from the scene
   simply does nothing - no error, no warning. Now 27 call sites:
   ArmoryManager 10, SfxPlayer 9, CameraShake 4, ImpactEffects 2, TracerPool 1,
   HitStop 1. Every new system adopted the pattern instead of the pattern
   being fixed. Has already cost several debugging round trips.
   Fix: startup validation on each singleton, and extend the builder's
   Validate step to assert every singleton, clip and prefab landed.

8. EVERY NEW SCENE COMPONENT NEEDS A BUILDER RE-RUN.
   Adding a component in code does nothing until ArenaBuilder is run again.
   Easy to forget, and the symptom is silent (see item 7). Now mitigated by
   SystemsCheck, and goes away entirely when the builder retires in Phase 3.

9. HIT TEST IS A RAY, NOT A SPHERECAST. - DEFERRED, deliberately.
   Reviewed 2026-09-08: switching to a spherecast would subtly widen every
   shot, changing hit feel that has now been validated through two gates. Not
   worth destabilising for a case that cannot currently occur. Fix it when an
   enemy actually closes inside the player's centre, not before.
   Shots are correct now that the ray starts at the player's centre, but an
   enemy that closes inside that centre would still be missed. Cannot happen
   at current values - zombies stop at 1.6 units - but a fast archetype in
   Phase 1 could. Switch to SphereCast when that enemy exists.

10. VFX SIZES ARE TUNED TO ONE CAMERA DISTANCE.
   Sparks and shell casings are sized in world units for a camera 22.8 units
   out (~45 px per unit). Any change to camera height or FOV silently changes
   how readable they are. Revisit if the camera framing moves in Phase 2.
   The light art pass is exactly when that would happen.

11. WEAPON ASSETS ARE OVERWRITTEN EVERY BUILD. EVERY OTHER ASSET IS NOT.
   REWRITTEN 2026-09-10: this entry previously said the opposite, and was
   simply wrong. LoadOrCreateWeapon applies its configure block OUTSIDE the
   null check, so every builder run overwrites every weapon definition with
   the values in code. Hand-tuning a weapon in the inspector does NOT survive
   a rebuild, and the register said it did.
   Everything else is genuinely create-if-missing and returns early when the
   asset exists: upgrade trees, deliveries, deployables and ArmoryPrices. So
   the project has two opposite contracts with no naming to tell them apart,
   and the one that loses work is the one that was documented as safe.
   Not fixed yet because both behaviours are defensible - weapons live in code
   and want to stay in step with it, tuned economy values want to survive. The
   fix is to make the difference visible: name them apart (LoadOrCreate vs
   WriteWeapon), and say in the tooltip which is which.

12. WEAPON MODS ARE HARDCODED BY SLOT INDEX. - RESOLVED 2026-09-08.
   WeaponDefinition now carries WeaponTags (Sidearm / Shotgun / Rifle /
   Precision) plus its own Overclocked response values, and
   ArmoryManager.ModAppliesTo owns the mod-to-tag rules. Weapon.cs has no
   slot-index branching left; slotIndex survives only for reserve ammo
   lookup, which is legitimately positional. Reordering slots or adding a
   fifth weapon can no longer mis-assign a mod.
   Original entry follows.
   ---
   Weapon.cs branches on slotIndex == 0, isShotgun = slotIndex == 1, and
   slotIndex == 2 || slotIndex == 3. Reordering the loadout silently
   reassigns every mod to the wrong gun, and a fifth weapon means editing
   Weapon.cs. This directly undoes the point of moving weapons into
   ScriptableObjects one commit earlier.
   Fix: mods declare what they apply to, or WeaponDefinition carries tags
   (shotgun / precision / sidearm) the mods match against.

13. ARMORY PRICES ARE COMPILE-TIME CONSTANTS. - RESOLVED 2026-09-08.
   Costs live in an ArmoryPrices ScriptableObject, created once and then left
   alone on the same create-if-missing contract as the weapon definitions, so
   tuning survives a rebuild. ArmoryManager keeps static passthrough
   properties, so all ~20 UI call sites are unchanged.
   Original entry follows.
   ---
   ArmoryManager holds every cost as const int, so balancing the economy is a
   recompile rather than an edit. Painful once run structure is fixed and the
   economy actually needs tuning against it.
   Fix: move costs into a ScriptableObject alongside the weapon definitions.

15. MOD CORES ARE AN ENUM WITH HARDCODED BRANCHES. -- RESOLVED 2026-09-09.
   ModCoreType was an enum with every effect a branch in Weapon.cs, and it
   blocked the upgrades task: 60 upgrades would have meant 60 branches in one
   file. Resolved by DELETION rather than by the planned migration into
   ScriptableObjects - upgrade trees already did everything mods did, and
   keeping both meant two systems making a weapon stronger with neither aware
   of the other and untested stacking between them.
   Worth recording that deleting was cheaper than migrating and only became
   the obvious answer once the replacement existed. The register had assumed
   a rework for weeks.

16. WEAPON LOADOUT IS FIXED AT FOUR SLOTS. -- RESOLVED 2026-09-09.
   Split into a catalogue (everything sellable) and a carried array (what the
   run holds), so a slot is live simply by holding something rather than by a
   parallel unlocked[] flag. Carry capacity is 10 on the number row.
   It was filed as "a design choice rather than a defect", and that was right:
   it did not become answerable until the direction settled on one path per
   weapon, at which point the answer was obvious. Some register items are
   waiting on a decision, not on effort.

17. UPGRADE BEHAVIOUR FLAGS ARE A FLAT, GROWING VOCABULARY.
   UpgradeTier carries around 30 fields now - dualWield, fanFireRate,
   critChance, focusBonusPerHit, executeThreshold and so on - each read by an
   if in Weapon.cs. New tiers and whole new paths are still pure data, which
   was the point. But a new KIND of effect costs a field, a WeaponStats
   member, a line in UpgradeManager.Apply and a branch in the firing path.
   Four authored paths in, there is a useful distinction the raw count hides:
   of the seven fields the shotgun/rifle/sniper paths needed, THREE were plain
   stat overrides that should have existed from the start (pelletCount,
   spread, penetrationFalloff) and only Focus was a genuinely new mechanic.
   The abstraction to design is narrower than 30 fields suggests - most of the
   growth is stats the resolver simply did not cover yet.
   UPDATED 2026-09-10, and the pattern changed. The vocabulary is around 46
   fields now. Every batch until the last one grew it with plain STAT
   OVERRIDES - pelletCount, spread, range, cone angle, chain hops - things the
   resolver should have covered from the start. The final three weapons grew it
   with genuine NEW MECHANICS instead: lifesteal, pinning, barricade repair,
   kill stacks, a revive.
   That distinction is the design brief for the rework. The stat half wants a
   generic override table and would collapse most of the field count. The
   mechanic half genuinely is behaviour and probably wants to stay explicit.
   Splitting them is the work; treating all 46 as one problem is why it has
   looked too big to start.
   Still deliberately unfixed - it costs real churn across ten authored paths
   and buys the player nothing. After Gate 2.

18. GAMEPAD CANNOT REACH WEAPON SLOTS 5-10.
   The number row maps all ten; the d-pad has four directions and a pad has no
   tenth face button. Gamepad players can select the first four weapons only.
   Fix: bind cycling (shoulder buttons) rather than direct selection, which
   also wants the HUD to show the carried list rather than just the current
   weapon. Not urgent while the game is keyboard-first in development, but it
   is a real parity gap and it grows as weapons are added.

19. WEAPON.CS IS HITSCAN ONLY. -- RESOLVED 2026-09-09.
   Delivery is now a WeaponDelivery ScriptableObject on the definition, with
   Hitscan, Cone, Chain and Projectile implementations. A weapon that leaves
   the field empty falls back to hitscan, so the first four needed no assets.
   Worth recording that this was called correctly in advance: it was filed as
   shared infrastructure to build once rather than six times, and building it
   first meant the abstraction was designed against four real weapon shapes
   instead of one. Compare debt 17, which is still unfixed for the opposite
   reason - there were never enough examples to generalise from.

20. PROJECTILE KILLS DO NOT ROUTE THROUGH WEAPON.ONKILL.
   A projectile carries its damage rather than asking the weapon at impact,
   because by the time it lands the player may have swapped weapons, bought a
   tier or died - the shot has to be worth what it was worth when it was
   fired. The consequence is that kills it scores charge no ultimate and
   refund no reserve.
   Harmless for the Grenade Launcher, and NOT worth fixing speculatively.
   IT ALMOST BIT ON 2026-09-10: the Nail Gun was authored as a projectile
   weapon, and its whole path - repair, pinning - lives in ApplyShot, which
   projectiles bypass. The path would have been silently inert. Caught in
   review, and the weapon is hitscan now.
   So the entry is doing its job, but only because someone happened to reread
   it. Anything reaching for ProjectileDelivery should check this first: if the
   weapon's upgrades do ANYTHING on hit beyond damage, projectiles are the
   wrong delivery until this is fixed.

24. FIELDS SILENTLY IGNORES UNKNOWN PROPERTY NAMES. -- FILED IN ERROR.
   WITHDRAWN 2026-09-10, the day it was filed. Fields.Find already logs
   "ArenaBuilder: '<Type>.<field>' not found - renamed?" and has done since
   the first commit. The projectileDamage typo that prompted this would have
   appeared in the console on the next builder run; it was never silent.
   Kept rather than deleted because the mistake is the useful part: the entry
   was written from memory of what the helper probably did, without opening
   it. A register is trusted precisely because nobody re-derives it, so an
   entry added without checking is worse than no entry - and this one was
   found only because someone asked which debt to pay next.
   Standing rule from it: verify against the code before adding to this file,
   the same as before changing it.

23. THE SAME MISTAKE HAS NOW HAPPENED THREE TIMES.
   ModCoreType was an enum with a branch per mod. WeaponLoadout was four fixed
   slots with a parallel unlocked[]. WaveManager was four prefab fields, three
   start-wave ints and a weight formula with a branch per type. Every one of
   them blocked a roster expansion, every one was found only when the roster
   needed to grow, and every one was fixed the same way - move the set into
   data and delete the branches.
   The tell is specific and easy to check for: CONTENT STORED AS A FIXED,
   SMALL NUMBER OF SERIALIZED FIELDS, with code that names each one. Deployable
   definitions and the weapon catalogue are what the right version looks like.
   Places this pattern still exists, unblocked only because nothing has needed
   them to grow yet: the four ArmoryUI deployable buttons are wired by index,
   and the boss is a single bossPrefab field on WaveManager. Neither is worth
   fixing today; both are worth remembering before the second boss.

22. THE FLAMETHROWER HAS NO SOUND.
   It fires ten times a second, so the shared gunshot clip machine-guns
   through the voice pool and reads as a stutter rather than a flame. Its
   fireClip and impactClip are deliberately null, which makes it silent.
   Fix: a looping sample with a proper loop point, started on first fire and
   faded out on release - which means SfxPlayer needs a concept of a held
   loop, not just one-shots. Any continuous weapon will want the same, so do
   it once. It WILL feel wrong to play before then; that is this entry, not a
   bug.

21. A CONDITIONAL COMMENT IS NOT A REMINDER.
   ZombieAI's own class comment said "the arena is open... swap in a
   NavMeshAgent if the map ever gains real obstacles". The arena gained
   fourteen cover blocks when it was enlarged on 2026-09-08, and nothing
   revisited the steering. The bug shipped into every play-test for a day
   before it was reported, and the code had described the exact precondition
   for its own failure the whole time.
   The lesson is not "write better comments" - that comment was excellent. It
   is that an assumption with an expiry condition has no owner unless it is in
   this file. When a comment says "revisit X if Y", Y belongs in the register
   or in the task that would cause it.

14. NEVER HAND-EDIT ASSET YAML FOR FIELDS THAT ARE NOT COMPILED YET.
   Learned the hard way 2026-09-08. Weapon tags were written straight into the
   .asset files while Unity was still compiling the WeaponDefinition change,
   so on reimport the keys were unknown and Unity silently discarded all
   twelve of them. Four of the five mods went dead - they check tags that were
   suddenly None - and nothing reported it, because the drum mags mod applies
   unconditionally and so still appeared to work.
   Rule: let Unity compile the field first, then write the value, then verify
   it survived a reimport. For anything larger than a few keys, use an editor
   script with SerializedObject instead of touching YAML - that path cannot
   write a field the assembly does not have.


-------------------------------------------------------------------------------
RESOLVED
-------------------------------------------------------------------------------

ZOMBIE STEERING VS DEPLOYABLE BARRICADES - resolved 2026-09-08.
   Zombies sense a blocking barricade ahead and claw it down on their attack
   cooldown, while separation slides adjacent horde members along the wall.
   On collapse the path clears and they advance. A barricade redirects a
   crowd rather than freezing it.
   (This was briefly filed as debt item 6, displacing the O(n^2) entry that
   belongs there. Recorded here instead.)


===============================================================================
START HERE - THIS WEEK
===============================================================================

1. Play all four upgrade paths, and answer one question with them.
   Nothing here has been played: the panel, the tiers, the ultimate, the
   second pistol, the ten-slot loadout, the repriced shop. A full path is 3360
   gold, so reaching T5 once is itself the first real test of the economy.
   The question worth answering before six more weapons exist: DOES ONE PATH
   PER WEAPON FEEL LIKE ENOUGH? Cheaper to learn now with four built than
   with ten.
   Specific numbers to watch: the 0.2s tap-versus-hold threshold on pistol T3,
   the ultimate's 2.0-second duration on pistol T5, sniper overpenetration
   compounding to ~9x on the twelfth body, and the sniper execute taking the
   boss's last 600 HP for free.

2. Play six new weapons and eight new archetypes.
   Easily the largest untested batch so far, and now the whole roster. The weapons are the first things
   to run cone, chain and projectile, so expect MODE bugs rather than balance
   problems: flame ignoring walls, an arc that picks nothing, a grenade
   arming at your feet.
   Archetypes to watch hardest: the Sapper (a 4x barricade multiplier could
   make walls pointless, and a 26m search radius could have it crossing the
   map past a defenceless player), the Leaper (arc bugs - landing inside
   geometry, or vaulting nothing), and Screamer plus Crawler together, which
   could be genuinely unfair.

3. The last three weapons.
   SMG and Siphon Rifle are plain hitscan. The Nail Gun needs pinning, which
   is upgrade-path code rather than delivery infrastructure. The Siphon Rifle
   is the only healing in the entire game and the Nail Gun the only weapon
   that touches the barricade pillar, so neither is just another gun.

4. Meta progression and the horde performance pass.

5. Then: meta progression and the horde performance pass.
   Meta progression needs the save system (debt 4). Performance is debt 6,
   deliberately deferred to exactly this point - and the horde is now twelve
   archetypes rather than four, several of which run their own per-frame
   searches, so this is the moment the O(n^2) separation stops being free.

6. GATE 2 after that: 10+ hours without repetition becoming obvious, and no
   run feeling like a strictly worse version of another.

STILL OPEN, kept for the specifics:
   The arena and deployables have not been played either. Two things to watch:
   the horde may feel thin, since maxAliveAtOnce is still 60 spread over 2.25x
   the space, and barrels damaging the player may prove more annoying than
   tense - both are single values to change. Buy a couple of each deployable
   on the first break; you start with one barricade and none of the explosives.

OLD ITEMS, kept for the reasoning:
   Play wave 15 and tune the boss.
   Three numbers are low-confidence. 2000 HP against a full wave 15 could be
   trivial with a saved-up loadout or brutal without one. Phase 1 may be
   invisible - a ranged attacker sitting at 14 units during a horde fight may
   simply never get noticed. And the artillery is currently just the Ranged
   archetype's straight projectile; the telegraphed ground-marker AoE is not
   built, so phase 1 may feel thin until it is.

2. Then: the artillery telegraph.
   A ground marker before impact. Wave 15 plus a boss is the most crowded the
   screen ever gets, and an untelegraphed area attack in that is unreadable.

OLD ITEM, now done - kept for the reasoning about ordering:
   Build the boss - and do it BEFORE retiring ArenaBuilder.
   Phase 2 retires the builder when the arena becomes hand-authored, and that
   is a one-way door: after it, every new prefab and wiring is manual scene
   work. The boss is the last thing Phase 1 owes, and it is much cheaper to
   add while the builder still assembles the scene. It also makes the Gate 2
   store-page clip dramatically stronger - a 60-second capture ending on a
   boss reads as a game, one ending on wave 7 reads as a prototype.
   A new enemy prefab, telegraphed wind-up attacks the player can read and
   react to, at least one phase change, a health bar, and the win trigger
   moving from wave-cleared to boss-killed. The thing to avoid is a boss that
   is only a bigger Brute - you already have that enemy. It should attack the
   fortification hook directly, for example by smashing barricades in a single
   hit, so walls buy time rather than safety.

   Then Phase 2 - the light art pass. SUPERSEDED: all art moved to Phase 3.

4. Debt: register audited against the code 2026-09-10.
   Two entries were wrong. Item 11 claimed weapon hand-tuning survives a
   rebuild when the builder overwrites weapon assets every run - the failure
   mode that entry protected against was real, and pointed the wrong way.
   Item 24 described a silent failure in Fields that does not exist.
   Both were caught by reading the code rather than the register. Everything
   else in the file above the resolved entries has now been checked against
   what it describes.

3. Debt: reviewed 2026-09-08.
   Fixed: 7 (silent singletons), 12 (mods hardcoded by slot), 13 (prices as
   constants). Deliberately deferred with reasons recorded: 5 (asmdefs, not
   costing time yet), 6 (O(n^2), trivial at 60 agents - Phase 4), 9
   (spherecast, would alter validated hit feel). The rest are phase-scheduled
   or by design. Nothing in the register is now unexamined.
